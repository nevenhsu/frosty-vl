# Internal / never publish
RETRIEVED/

# Build artifacts / archives (delivered as release assets, not tracked)
*.tar.gz
*.tar.gz.sha256
SHA256SUMS

# Env & secrets
.env
.env.*

# Python
__pycache__/
*.pyc
.DS_Store
node_modules/
.pytest_cache/
outputs/
*.safetensors
*.gguf

# Proprietary IP — direction vectors are delivered to licensed customers,
# NOT committed to the public repo. Remove these lines only to publish them.
server/data/*.npy
server/data/*.json

# ComfyUI node vectors — delivered to licensed customers, not public
comfyui/data/*.npy
comfyui/data/*.json

# Optional MCP virtual environment
.venv-mcp/

# Optional ComfyUI adapter environment
.venv-comfy/
